// Stub API for booking/demo requests (no backend)
export const createDemoRequest = async (requestData) => {
    console.log('Demo request submission not available without backend:', requestData);
    return null;
};

export const getDemoRequests = async () => {
    return [];
};
