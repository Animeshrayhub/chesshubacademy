// Stub API for leads (no backend)
export const createLead = async (leadData) => {
    console.log('Lead submission not available without backend:', leadData);
    return null;
};

export const getLeads = async () => {
    return [];
};
