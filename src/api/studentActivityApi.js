// Stub API for student activity (no backend)
export const logActivity = async (studentId, activityType, data) => {
    console.log('Activity logging not available without backend');
    return null;
};

export const getStudentActivity = async (studentId) => {
    return [];
};
